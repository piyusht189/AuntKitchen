<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.almanac.piyush.auntkitchen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Choose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".custlogin" />
        <activity android:name=".auntlogin" />
        <activity
            android:name=".aorders"
            android:label="@string/title_activity_aorders"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".atoday"
            android:label="@string/title_activity_atoday"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".aprofile"
            android:label="@string/title_activity_aprofile"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>